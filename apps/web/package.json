{
  "name": "@lease-bot/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port ${WEB_PORT:-5173}",
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port ${WEB_PORT:-5173}"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
