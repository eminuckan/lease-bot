# CI Run Metadata

Copy this document from the `ci-run-metadata` artifact generated by the `Critical Path Gates` workflow.
Do not hand-edit check statuses; they must match the workflow artifact for the release candidate commit.

## Release candidate

- Date (UTC):
- Branch/Ref:
- Commit SHA:
- Workflow: Critical Path Gates
- Run ID:
- Run URL:

## Required workflow checks

| Check | Job status | Evidence artifact |
| --- | --- | --- |
| API tests (`npm test -w @lease-bot/api`) | | api-tests-log |
| Worker tests (`npm test -w @lease-bot/worker`) | | worker-tests-log |
| Web smoke (`npm run smoke -w @lease-bot/web`) | | web-smoke-log |

## Notes

- Branch protection gate result:
- Any reruns needed:
- Incident/ref links:
